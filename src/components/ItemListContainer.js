
const ItemListContainer = (props) => {
    return (
        <main>
                <h2 className="text-center size-2 my-1">{props.saludo}</h2>
        </main>
    );
}

export default ItemListContainer;